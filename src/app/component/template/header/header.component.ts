import { Component } from '@angular/core';

/**
 * Componente responsável pelo cabeçalho da aplicação.
 */
@Component({
  selector: 'app-header', // Seletor do componente
  templateUrl: './header.component.html', // Caminho para o template HTML
  styleUrls: ['./header.component.css'] // Caminho para os estilos CSS
})
export class HeaderComponent {
  // Componente sem lógica adicional no momento
}